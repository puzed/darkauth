{
  "name": "@DarkAuth/test-suite",
  "version": "1.0.0",
  "license": "AGPL-3.0",
  "private": true,
  "description": "End-to-end test suite for DarkAuth",
  "type": "module",
  "scripts": {
    "pretest": "npm --workspace @darkauth/client run build && npm --workspace @DarkAuth/admin-ui run build && npm --workspace @DarkAuth/user-ui run build && npm --workspace @DarkAuth/demo-app run build",
    "test": "npm run pretest && PW_REPORTER=dot PW_ARTIFACTS=off npx playwright test --reporter=dot",
    "test:screenshots": "rm -rf test-results-light test-results-dark && npm run pretest && (COLOR_SCHEME=light PW_REPORTER=dot PW_ARTIFACTS=on npx playwright test --reporter=dot --workers=1 --output=test-results-light) && (COLOR_SCHEME=dark PW_REPORTER=dot PW_ARTIFACTS=on npx playwright test --reporter=dot --workers=1 --output=test-results-dark) && node ../brochureware/scripts/collect-screenshots.js",
    "test:report": "npm run pretest && PW_REPORTER=html PW_ARTIFACTS=on npx playwright test --reporter=html && npx playwright show-report",
    "install:playwright": "npx playwright install"
  },
  "dependencies": {
    "@DarkAuth/api": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.9",
    "drizzle-orm": "^0.45.1",
    "pg": "^8.17.1",
    "typescript": "^5.9.3"
  }
}
